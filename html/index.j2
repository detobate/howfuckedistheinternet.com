<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <title>How Fucked Is The Internet?</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" type="text/css" href="style.css">
    <meta name="description" content="Definitively measuring the fuckedness of the Internet">
    <meta http-equiv="refresh" content="300">
</head>
<body>
<header>
    <p class="metadata">
    Fuckedness last checked {{ timestamp }}</br>
    It took {{ duration }} seconds to check the fuckedness
    </p>
</header>
<main class="wrapper">
    <section class="fuckometer">
        <h1>{{ status }}</h1>
    </section>
    <section class="why">
        <h2>But why though?</h2>
        {% if metrics -%}
        <h3>Metrics</h3>
        <ul class="how">
            {% for metric in metrics -%}
            <li>{{ metric['description'] }} (weight:{{ metric['weight'] }})
            {% endfor -%}
        </ul>
        {% endif -%}
        {% if reasons -%}
        <h3>Specifically</h3>
            {% for reason in reasons -%}
            <li><var>{{ reason[0] }} (weight:{{ reason[2] }})</var>
            {% endfor -%}
        </ul>
        {% endif -%}
    </section>
</main>
</body>
</html>
